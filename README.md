This folder contains a program that takes a text input, in this case an exert from Frankensteins monster, tokenizes every word and generates a random sentence from those tokens. 
Currently it has a bug where it will only generate sentences of two words. 
